
  <input 
    type="{{ $type }}"
    name="{{ $name }}"
    id="{{ $id }}"
    placeholder="{{ $placeholder }}"
    value="{{ $value }}"
    maxlength="{{ $maxlength }}"
    required
    class="form-control"
  />
<br>

